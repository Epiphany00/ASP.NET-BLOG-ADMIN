@model IEnumerable<TestProject.Models.Service>
@{
    ViewBag.Title = "Services";
    Layout = "~/Views/Shared/_SiteLayout.cshtml";
}


<section id="content">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">

                <div class="row">
                    <section id="projects">
                        <ul id="thumbs" class="portfolio">
                            <!-- Item Project and Filter Name -->
                            @*
                            @foreach (var item in Model)
                            {
                                <li class="item-thumbs col-lg-3 design" data-id="id-0" data-type="web">
                                    <!-- Fancybox - Gallery Enabled - Title - Full Image -->
                                    <a class="hover-wrap fancybox" data-fancybox-group="gallery" title="@item.Title" href="@item.PictureURL">
                                        <span class="overlay-img"></span>
                                        <span class="overlay-img-thumb font-icon-plus"></span>
                                    </a>
                                    <!-- Thumb Image and Description -->
                                    <img src="@item.PictureURL" alt="@item.Description" width="360" height="200">
                                </li>
                            }
*@

                            @foreach (var tmp in Model)
                            {
                                <li class="col-lg-3 design" data-id="id-0" data-type="web">
                                    <div class="item-thumbs">
                                        <!-- Fancybox - Gallery Enabled - Title - Full Image -->
                                        <a class="hover-wrap fancybox"  data-fancybox-group="gallery" title="@tmp.Title  @tmp.Description" href="">
                                            <img src="@tmp.PictureURL " height="200" width="360" />
                                            <span class="overlay-img"></span>
                                            <span class="overlay-img-thumb "></span>
                                        </a>
                                        <!-- Thumb Image and Description -->
                                        <img src="@tmp.PictureURL" height="100" width="100" alt="@tmp.Description">
                                    </div>
                                </li>

                            }


                            <!-- End Item Project -->

                        </ul>
                    </section>
                </div>
            </div>
        </div>
    </div>
</section>

